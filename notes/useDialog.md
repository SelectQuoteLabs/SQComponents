# useDialog

## Examples

```
import {useDialog} from 'scplus-shared-components;
...
const [isDialogOpen, {openDialog, closeDialog}] = useDialog()

// OR rename to something more specific

const [isFormDialogOpen, {openDialg: openFormDialog, closeDialog: closeFormDialog}] = useDialog()

```
